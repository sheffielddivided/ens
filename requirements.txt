# Runtime dependencies for the ENS oil/gas/water production pipeline.
# Pin to compatible ranges; the pipeline is exercised on Python 3.11+.
requests>=2.31,<3
beautifulsoup4>=4.12,<5
lxml>=5.0,<6            # fast, lenient HTML/XML parser used by BeautifulSoup
pandas>=2.1,<3
openpyxl>=3.1,<4       # .xlsx reading for pandas
pdfplumber>=0.11,<0.12 # primary PDF table extractor

# Test-only dependency (kept here so CI installs a single file).
pytest>=8.0,<9
