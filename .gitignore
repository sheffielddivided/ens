# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# OS
.DS_Store

# Local scratch / preview data generated by --demo (never the real committed data)
/data/combined.sample.local.json

# NOTE: data/*.json and data/sources/raw/* ARE tracked on purpose.
# The pipeline commits fetched raw sources and derived JSON so parsing is
# reproducible and the history is auditable.

# NOTE: the ENS map shape files carry no redistribution restrictions, so the
# raw .zip inputs under data/sources/gis/raw/ ARE tracked too (like the
# production sources) -- the reprojected GeoJSON under docs/data/gis/ is derived
# from them and both are committed for reproducibility.
